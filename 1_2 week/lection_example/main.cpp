#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    float exp;
    float e = 2.718;
    int x,z;
    float k = (e^(M_PI*x))*cos(0.01*z);
    float y = (sqrt(1/(exp(k))+10^3)-sqrt(10^3-1/(exp(k))))/(x*x+18*x-40);

    std::cin >> x >> z >> std::endl;
    std::cout << y= << y << std::endl;


    return 0;
}

#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int x;
    float y;
    std::cin >> x;
    if (x > 5)
        std::cout << "y= " << y = 3*x-5;
    if (x = 5)
        std::cout << "y= " << y = 10;
    if (x < 5)
        std::cout << "y= " << y = 2*fabs(x-2)+5;
    std::cout << y << std:: endl;
    std::getchar();
    return 0;
}
